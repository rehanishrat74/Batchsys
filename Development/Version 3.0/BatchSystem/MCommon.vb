Imports InfiniImageVB
Imports System.Xml
Imports Microsoft.Win32
Imports System.IO

Module MCommon

    'Public Sub WriteLog(ByVal message As String, ByVal folder As String, ByVal fileName As String)
    '    Dim dir As New System.IO.DirectoryInfo(folder)
    '    Dim fle As New System.IO.FileInfo(folder & "\" & fileName)
    '    Dim sw1 As StreamWriter

    '    If Not dir.Exists() Then
    '        dir.Create()
    '    End If
    '    If Not fle.Exists Then
    '        sw1 = New StreamWriter(File.Open(Application.StartupPath() & "\Trace\Log.txt", FileMode.Create))
    '    Else
    '        sw1 = New StreamWriter(File.Open(Application.StartupPath() & "\Trace\Log.txt", FileMode.Append))
    '    End If
    '    sw1.WriteLine(".Net : btnSearch_Click : Search button click on Port Seting Form ")
    '    sw1.Close()
    'End Sub
End Module
